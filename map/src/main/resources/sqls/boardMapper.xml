<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board">

	<insert id="insert" parameterType="BoardDto">
		INSERT INTO TBL_BOARD (BNO, WRITER, RECEIVER, TITLE, CONTENT)VALUES(
			SEQ_BOARD.NEXTVAL, #{writer}, #{receiver}, #{title}, #{content}
		)
	</insert>
	
	<select id="read" resultType="BoardDto">
		SELECT * FROM TBL_BOARD WHERE BNO = #{bno}
	</select>
	
	<sql id="criteria">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach item="type" collection="typeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							TITLE LIKE '%' || #{keyword} || '%'
						</when>
						<when test="type == 'C'.toString()">
							CONTENT LIKE '%' || #{keyword} || '%'
						</when>
						<when test="type == 'R'.toString()">
							RECEIVER LIKE '%' || #{keyword} || '%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>
	
	
	<select id="getList" resultType="BoardDto">
	<![CDATA[
		SELECT BNO, WRITER, RECEIVER, TITLE, CONTENT, REGDATE
		FROM
		(
		SELECT /*+INDEX_DESC(TBL_BOARD PK_BOARD)*/
		ROWNUM RN, BNO, WRITER, RECEIVER, TITLE, CONTENT, REGDATE
		FROM
			TBL_BOARD
		WHERE
		]]>
		
		<include refid="criteria"/>	
		
		<![CDATA[
			
			ROWNUM <= #{pageNum} * #{amount}
		)
		WHERE RN > (#{pageNum} -1) * #{amount}
	]]>
	
	</select>
	
	<update id="update" parameterType="BoardDto">
		UPDATE TBL_BOARD 
		SET WRITER = #{writer},
		RECEIVER = #{receiver},
		TITLE = #{title},
		CONTENT = #{content}
		WHERE BNO = #{bno}
	</update>
	
	<delete id="delete">
		DELETE FROM TBL_BOARD WHERE BNO = #{bno}
	</delete>
	
	<select id="getTotalCount" resultType="int">
		SELECT COUNT(*) FROM TBL_BOARD WHERE
		
		<include refid="criteria"/>
		
		BNO > 0
	</select>

</mapper>
